#include <stdio.h>
#include <stdlib.h>

typedef struct{
    int contenu;
    struct element *suivant}Element;

function Element *create(inta){}
    int isEmpty(Element*);
    void 
    // creation elements disperses dans la meamoire
    Element *create *e;